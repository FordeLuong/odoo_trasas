<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- FORM VIEW: Yêu cầu truy cập -->
    <record id="trasas_doc_access_request_view_form" model="ir.ui.view">
        <field name="name">trasas.doc.access.request.form</field>
        <field name="model">trasas.doc.access.request</field>
        <field name="arch" type="xml">
            <form string="Yêu cầu truy cập tài liệu">
                <header>
                    <button name="action_submit" string="Gửi yêu cầu" 
                            type="object" class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_approve" string="Phê duyệt" 
                            type="object" class="btn-success"
                            invisible="state != 'submitted'"
                            groups="trasas_document_management.group_doc_manager"/>
                    <button name="action_reject" string="Từ chối" 
                            type="object" class="btn-danger"
                            invisible="state != 'submitted'"
                            groups="trasas_document_management.group_doc_manager"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="user_id" readonly="state != 'draft'"/>
                            <field name="department_id" readonly="state != 'draft'"/>
                            <field name="access_type" readonly="state != 'draft'"/>
                            <field name="access_duration" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="approved_by" invisible="state not in ('approved', 'expired')"/>
                            <field name="access_start_date" invisible="state not in ('approved', 'expired')"/>
                            <field name="access_expiry_date" invisible="state not in ('approved', 'expired')"/>
                        </group>
                    </group>

                    <group string="Tài liệu cần truy cập">
                        <field name="document_ids" widget="many2many_tags" nolabel="1" colspan="2" readonly="state != 'draft'"/>
                    </group>

                    <group string="Thư mục cần truy cập (tuỳ chọn)">
                        <field name="folder_id" readonly="state != 'draft'"/>
                    </group>

                    <group string="Mục đích truy cập">
                        <field name="purpose" nolabel="1" colspan="2" readonly="state != 'draft'" placeholder="Ghi rõ lý do cần truy cập tài liệu..."/>
                    </group>

                    <group string="Lý do từ chối" invisible="state != 'rejected'">
                        <field name="reject_reason" nolabel="1" colspan="2"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- LIST VIEW: Yêu cầu truy cập -->
    <record id="trasas_doc_access_request_view_list" model="ir.ui.view">
        <field name="name">trasas.doc.access.request.list</field>
        <field name="model">trasas.doc.access.request</field>
        <field name="arch" type="xml">
            <list string="Yêu cầu truy cập" 
                  decoration-warning="state == 'submitted'"
                  decoration-success="state == 'approved'"
                  decoration-danger="state == 'rejected'"
                  decoration-muted="state == 'expired'">
                <field name="name"/>
                <field name="user_id" widget="many2one_avatar_user"/>
                <field name="department_id"/>
                <field name="access_type"/>
                <field name="access_duration"/>
                <field name="access_expiry_date"/>
                <field name="state" widget="badge" 
                       decoration-info="state == 'draft'"
                       decoration-warning="state == 'submitted'"
                       decoration-success="state == 'approved'"
                       decoration-danger="state == 'rejected'"
                       decoration-muted="state == 'expired'"/>
            </list>
        </field>
    </record>

    <!-- SEARCH VIEW: Yêu cầu truy cập -->
    <record id="trasas_doc_access_request_view_search" model="ir.ui.view">
        <field name="name">trasas.doc.access.request.search</field>
        <field name="model">trasas.doc.access.request</field>
        <field name="arch" type="xml">
            <search string="Tìm yêu cầu truy cập">
                <field name="name"/>
                <field name="user_id"/>
                <separator/>
                <filter string="Chờ duyệt" name="filter_submitted" domain="[('state', '=', 'submitted')]"/>
                <filter string="Đã duyệt" name="filter_approved" domain="[('state', '=', 'approved')]"/>
                <filter string="Từ chối" name="filter_rejected" domain="[('state', '=', 'rejected')]"/>
                <filter string="Hết hạn" name="filter_expired" domain="[('state', '=', 'expired')]"/>
                <separator/>
                <filter string="Trạng thái" name="group_by_state" context="{'group_by':'state'}"/>
                <filter string="Người yêu cầu" name="group_by_user" context="{'group_by':'user_id'}"/>
            </search>
        </field>
    </record>

    <!-- ACTION: Yêu cầu truy cập -->
    <record id="action_doc_access_request" model="ir.actions.act_window">
        <field name="name">Yêu cầu truy cập</field>
        <field name="res_model">trasas.doc.access.request</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="trasas_doc_access_request_view_search"/>
        <field name="context">{'search_default_filter_submitted': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Chưa có yêu cầu truy cập nào!
            </p>
            <p>Nhấn nút "Tạo" để gửi yêu cầu truy cập tài liệu.</p>
        </field>
    </record>
</odoo>
