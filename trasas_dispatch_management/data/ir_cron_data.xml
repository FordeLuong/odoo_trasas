<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_dispatch_check_overdue" model="ir.cron">
            <field name="name">Công văn đến: Kiểm tra quá hạn</field>
            <field name="model_id" ref="model_trasas_dispatch_incoming"/>
            <field name="state">code</field>
            <field name="code">model.check_overdue_deadline()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>

            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 08:00:00')"/>
        </record>
    </data>
</odoo>
