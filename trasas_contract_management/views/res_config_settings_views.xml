<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.contract</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Contract" string="Hợp đồng TRASAS" name="trasas_contract_management" groups="trasas_contract_management.group_contract_manager">
                    <block title="Cài đặt Hợp đồng" name="contract_management_setting_block">
                        <setting title="Nhắc nhở hết hạn" help="Cấu hình số ngày hệ thống sẽ gửi cảnh báo trước khi hợp đồng hết hạn.">
                            <div class="content-group">
                                <div class="mt16">
                                    <label for="contract_reminder_days" string="Số ngày nhắc trước"/>
                                    <field name="contract_reminder_days" class="oe_inline"/>
                                    <span> ngày</span>
                                </div>
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_contract_config_settings" model="ir.actions.act_window">
        <field name="name">Cài đặt</field>
        <field name="res_model">res.config.settings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'module' : 'trasas_contract_management', 'bin_size': False}</field>
    </record>
</odoo>
