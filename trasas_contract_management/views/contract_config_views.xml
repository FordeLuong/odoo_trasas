<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ============ REJECT REASONS ============ -->
    <record id="trasas_contract_reject_reason_view_list" model="ir.ui.view">
        <field name="name">trasas.contract.reject.reason.list</field>
        <field name="model">trasas.contract.reject.reason</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="action_contract_reject_reason" model="ir.actions.act_window">
        <field name="name">Lý do từ chối</field>
        <field name="res_model">trasas.contract.reject.reason</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Tạo lý do từ chối mẫu mới.
            </p>
        </field>
    </record>

    <!-- ============ CONTRACT TAGS ============ -->
    <record id="trasas_contract_tag_view_list" model="ir.ui.view">
        <field name="name">trasas.contract.tag.list</field>
        <field name="model">trasas.contract.tag</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="action_contract_tag" model="ir.actions.act_window">
        <field name="name">Thẻ hợp đồng</field>
        <field name="res_model">trasas.contract.tag</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Tạo thẻ hợp đồng mới.
            </p>
        </field>
    </record>
</odoo>
